extends layout

block content
  h1= title
  p Welcome to #{title}. My name is Sidhartha Gundarapu
  p PUG change in paragraph text and change can be seen
  br
  - var btnType = 'success'
  - var btnSize = 'xl'
  button(type='button' onclick="moreClaps()" class='btn btn-' + btnType + ' btn-' + btnSize) Press Me

  div(id="target") Clap for me

  script.
    var clap_count=0;
    function moreClaps(){
      clap_count =clap_count+2;
      console.log("moreClaps method implemented");
      document.getElementById("target").innerHTML = `Clap ${clap_count} times.`;
    }
    a(href="books.pug") books link